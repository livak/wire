<Window x:Class="Wire.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:Wire"
        Title="Žica" Height="371" Width="564">
    <Window.Resources>
        <local:ResultToBackgroundConverter x:Key="ResultToBackgroundConverter"></local:ResultToBackgroundConverter>
    </Window.Resources>
    <Grid>
        <StackPanel>
            <StackPanel Orientation="Horizontal">
                <Label Content="Traženi presjek:" HorizontalAlignment="Left"  VerticalAlignment="Top" Width="100"/>
                <TextBox Name="tbx_Presjek" Text="0.7209" HorizontalAlignment="Left" Height="23" VerticalAlignment="Top" Width="120" />
                <StackPanel Orientation="Horizontal">
                    <Label Content="Odaberi slojnost:"></Label>
                    <StackPanel Orientation="Horizontal">
                        <RadioButton Name="radio_Slojnost1" Content="1" IsChecked="True" VerticalAlignment="Center" Margin="0,0,10,0"/>
                        <RadioButton Content="2" VerticalAlignment="Center"/>
                    </StackPanel>
                </StackPanel>
            </StackPanel>
            <StackPanel Orientation="Horizontal">
                <Label Content="Broj zavoja:" HorizontalAlignment="Left"  VerticalAlignment="Top" Width="100"/>
                <TextBox Name="tbx_BrojZavoja" Text="45" HorizontalAlignment="Left" Height="23" VerticalAlignment="Top" Width="120" />
                <StackPanel Orientation="Horizontal">
                    <Label>Odaberi raspon promjera žica od:</Label>
                    <ComboBox Name="drop_From" SelectedIndex="4" ItemsSource="{Binding Source={x:Static local:MainWindow.Zice}}"></ComboBox>
                    <Label>do:</Label>
                    <ComboBox Name="drop_To" SelectedIndex="29" ItemsSource="{Binding Source={x:Static local:MainWindow.Zice}}"></ComboBox>
                </StackPanel>
            </StackPanel>
            <StackPanel Orientation="Horizontal">
                <Label Content="Površina utora:" HorizontalAlignment="Left"  VerticalAlignment="Top" Width="100"/>
                <TextBox Name="tbx_PovrsinaUtora" Text="80.1" HorizontalAlignment="Left" Height="23" VerticalAlignment="Top" Width="120" />
                <CheckBox Name="cbx_PrikaziSve" Content="Prikaži sve kombinacije" VerticalAlignment="Center" Margin="5,0,0,0"></CheckBox>
            </StackPanel>
            <StackPanel Orientation="Horizontal">
                <Label Content="Max odstupanje:" HorizontalAlignment="Left"  VerticalAlignment="Top" Width="100"/>
                <TextBox Name="tbx_MaxOdstupanje" Text="2" HorizontalAlignment="Left" Height="23" VerticalAlignment="Top" Width="120" />
            </StackPanel>
            <StackPanel Orientation="Horizontal">
                <Label Content="Max broj žica:" HorizontalAlignment="Left"  VerticalAlignment="Top" Width="100"/>
                <TextBox Name="tbx_MaxBrojZica" Text="10" HorizontalAlignment="Left" Height="23" VerticalAlignment="Top" Width="120" />
            </StackPanel>
        </StackPanel>
        <Button Content="Nađi žicu" HorizontalAlignment="Left" Margin="267,80,0,0" VerticalAlignment="Top" Width="75" Click="Button_Click"/>
        <DataGrid Name="grid_Rezultat" Margin="0,138,0,0" AutoGenerateColumns="False">
            <DataGrid.Columns>
                <DataGridTextColumn Binding="{Binding Zica}" Header="Žica"/>
                <DataGridTextColumn Binding="{Binding NoviPresjek, StringFormat={}{0:0.######}}" Header="Novi Presjek"/>
                <DataGridTextColumn Binding="{Binding Odstupanje, StringFormat={}{0:0.##}}" Header="Odstupanje"/>
                <DataGridTextColumn Binding="{Binding Punjenje, StringFormat={}{0:0.##}}" Header="Punjenje"/>
                <DataGridTextColumn Binding="{Binding Razmak}" Header="Razmak"/>
            </DataGrid.Columns>
            <DataGrid.RowStyle>
                <Style TargetType="DataGridRow">
                    <Setter Property="Background" Value="{Binding RelativeSource={RelativeSource Self},
                        Path=Item.Razmak, Converter={StaticResource ResultToBackgroundConverter}}"/>
                </Style>
            </DataGrid.RowStyle>
        </DataGrid>

    </Grid>
</Window>
